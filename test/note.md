docker file encoding should be LF not CRLF

docker compose up -d

chezmoi docker exec <container id> horerr

docker run --rm --gpus 'all,"capabilities=compute,utility"' \
    nvidia/cuda:12.5.0-base-ubuntu22.04 nvidia-smi
